# Copy this file to .streamlit/secrets.toml and set a real password.
# secrets.toml itself is gitignored -- never commit the real password.
#
# On Streamlit Community Cloud, don't upload this file at all: open the
# app's dashboard, go to Settings -> Secrets, and paste the same
# app_password = "..." line there instead.

app_password = "change-me"
